#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int currentNumber = 0;
while (currentNumber <= 9)
{
printf("%d", currentNumber);
currentNumber++;
}
printf("\n");
return (0);
}
