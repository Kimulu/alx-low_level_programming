#include <ctype.h>
#include "main.h"
/**
*mul - Function for multiplying two numbers
*@a: The character to be used for multiplication
*@b: The character to be used for multiplication
*Return: returns the results
*/
int mul(int a, int b)
{
int v = (a * b);
return (v);
}
