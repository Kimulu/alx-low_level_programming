#include "main.h"
#include <stdio.h>
/**
 * add - a function that adds two numbers together
 * @k: integer value
 * @j: integer value
 * Return: Returns the sum
 */

int add(int k, int j)
{
int f = (k + j);
return(f);
}
