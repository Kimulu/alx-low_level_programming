#include <stdio.h>
#include "main.h"
#include <string.h>
/**
*_strlen - Function for returning the len of string
*@s: the string
*Return: return a number(length 0f string)from 0 to < 500
*/
int _strlen(char *s)
{
int length = strlen(s);
return (length);
}
