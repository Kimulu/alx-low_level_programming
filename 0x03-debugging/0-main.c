#include "main.h"

/**
 * main -  test if a function that checks is number is
 * positive or negative works then 0 is passed as the number
 * Return: 0 (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
